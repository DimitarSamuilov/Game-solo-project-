{% extends 'base.html.twig' %}

{% block main %}
    <div class="container body-content">
        <div class="well">
            <h2>Players bases </h2>
            <div class="row">
                <table class="table table-striped table-hover ">
                    <thead>
                    <tr>
                        <th>Base:</th>
                        <th>Belongs To:</th>
                        <td>Required Time:</td>
                        <th>Action:</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for base in bases %}
                        <tr>
                            <td>{{ base.id }}--[{{ base.x }}:{{ base.y }} ]</td>
                            <td>{{ base.userUsername }}</td>
                            <td>{{ base.time }}</td>
                            {% if base.battleTime==false %}
                                <td><a href="{{ path("fight_attack_menu",{id:base.id}) }}">Attack</a></td>
                            {% else %}
                                <td>{{ base.battleTime }}</td>
                            {% endif %}
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
